import React from 'react';
import styled from 'styled-components';

const Container = styled.div`
  width: 15%;
  height: auto;
`;

const ImgContainer = styled.div``;

const Name = styled.h3`
  span {
  }
`;

const Profile = ({ img, egName, koName }) => {
  // return (
  //   <Container>

  //   </Container>
  // );
  console.log(img);
};

export default Profile;
